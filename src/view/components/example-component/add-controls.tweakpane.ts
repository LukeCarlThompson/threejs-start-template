import type { ExampleComponent } from "./example-component";
import type { Pane } from "tweakpane";

export const addControls = (component: ExampleComponent, initialProps: ExampleComponent, pane: Pane): void => {
  // TODO: Add in some controls
  void component;
  void initialProps;
  void pane;
};
